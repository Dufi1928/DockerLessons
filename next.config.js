const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['vandb.fr','galliaparis.com'], // Assurez-vous d'ajouter 'www' si c'est une partie de l'URL de l'image.
  },
};

module.exports = nextConfig;
